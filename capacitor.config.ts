
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ganesh.trade',
  appName: 'GaneshTrade',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
